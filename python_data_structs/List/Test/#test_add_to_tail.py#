

def test(List, data):
    print "Testing add_to_tail...",
    for item in data:
        List.add_to_tail(item)
    for item in data:
        print item
        if item != List.dequeue()
            print "failed!"
            break
    print "passed!"
